/** Cookie set after the site-wide preview password is accepted. */
export const SITE_GATE_COOKIE = 'clikd_site_gate';

/** Shared unlock value stored in the cookie (not the password itself). */
export const SITE_GATE_COOKIE_VALUE = 'unlocked';

/** Preview password required before the rest of the site is reachable. */
export const SITE_GATE_PASSWORD = '1234';

export const SITE_GATE_PATH = '/gate';
export const SITE_GATE_API_PATH = '/api/gate';
